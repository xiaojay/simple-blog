# 简单博客 - Next.js 学习项目

这是一个使用 Next.js 构建的简单博客系统，专为初学者设计，用于学习和理解 Next.js 的基础概念。

## 项目特点

- ✨ 使用 Next.js 14+ App Router
- 💎 TypeScript 类型安全
- 🎨 Tailwind CSS 样式
- 📝 静态文章数据
- 🚀 简单易懂的代码结构

## 项目结构

```
simple-blog/
├── app/                    # Next.js App Router 目录
│   ├── lib/               # 工具函数和数据
│   │   └── posts.ts       # 文章数据和函数
│   ├── posts/             # 文章路由
│   │   └── [id]/          # 动态路由
│   │       └── page.tsx   # 文章详情页
│   ├── globals.css        # 全局样式
│   ├── layout.tsx         # 根布局
│   ├── page.tsx           # 首页（文章列表）
│   └── not-found.tsx      # 404 页面
├── package.json           # 项目依赖
├── tsconfig.json          # TypeScript 配置
├── tailwind.config.js     # Tailwind CSS 配置
└── next.config.js         # Next.js 配置
```

## 核心概念

### 1. App Router
使用 Next.js 13+ 的 App Router，基于文件系统的路由：
- `app/page.tsx` → 首页 `/`
- `app/posts/[id]/page.tsx` → 文章详情 `/posts/1`

### 2. 服务端组件
所有页面默认都是服务端组件，提供更好的性能和 SEO。

### 3. 静态生成
使用 `generateStaticParams` 在构建时生成所有文章页面。

### 4. TypeScript
为所有数据定义类型接口，提供类型安全。

## 快速开始

### 安装依赖

```bash
npm install
```

### 启动开发服务器

```bash
npm run dev
```

打开浏览器访问 [http://localhost:3000](http://localhost:3000)

### 构建生产版本

```bash
npm run build
npm start
```

## 如何添加新文章

编辑 `app/lib/posts.ts` 文件，在 `posts` 数组中添加新的文章对象：

```typescript
{
  id: '5',                              // 唯一 ID
  title: '你的文章标题',
  date: '2024-01-20',                   // 发布日期
  author: '作者名',
  excerpt: '文章摘要...',
  content: `
    # 文章内容

    这里是文章的完整内容...
  `,
  tags: ['标签1', '标签2'],
}
```

## 学习建议

1. **理解项目结构**：查看每个文件的作用和它们之间的关系
2. **修改样式**：尝试修改 Tailwind CSS 类名，看看效果
3. **添加新功能**：
   - 添加搜索功能
   - 按标签筛选文章
   - 添加评论功能
   - 添加分页功能
4. **改进 UI**：使用 Tailwind CSS 美化界面
5. **数据持久化**：尝试连接数据库或使用 Markdown 文件

## 技术栈

- [Next.js](https://nextjs.org/) - React 框架
- [React](https://react.dev/) - UI 库
- [TypeScript](https://www.typescriptlang.org/) - 类型系统
- [Tailwind CSS](https://tailwindcss.com/) - CSS 框架

## 下一步学习

- 学习如何使用 Markdown 文件存储文章
- 了解如何连接数据库（如 MongoDB、PostgreSQL）
- 学习 Next.js API Routes
- 添加用户认证功能
- 部署到 Vercel 或其他平台

## 许可证

MIT
